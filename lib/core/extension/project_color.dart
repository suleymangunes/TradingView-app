import 'package:flutter/material.dart';

extension ProjectColos on BuildContext {
  // haiti for background
  Color get haiti => const Color.fromRGBO(18, 21, 54, 1);
  // picton blue for selectem item
  Color get pictonBlue => const Color.fromRGBO(60, 227, 246, 1);
  // manatee for unselected item
  Color get manatee => const Color.fromRGBO(139, 141, 162, 1);
  // white for text
  Color get white => const Color.fromRGBO(255, 255, 255, 1);
  // dolly for icon
  Color get dolly => const Color.fromRGBO(254, 240, 141, 1);
  // martinique for button background
  Color get marinique => const Color.fromRGBO(42, 46, 75, 1);
  // cabaret for down price
  Color get cabaret => const Color.fromRGBO(212, 70, 86, 1);
  // jungle green up price
  Color get jungleGreen => const Color.fromRGBO(47, 165, 130, 1);
}
